# CLAUDE.md

Контекст проекта для Claude Code. Читается в начале каждой сессии. Правится вместе с сайтом.

## Что это за проект

`kholomyanskiy.com` — личный сайт консультанта по внедрению ИИ. Репозиторий `Kholomyanskiy/kholomyanskiy.github.io`, хостинг GitHub Pages, домен привязан, https работает.

Статический HTML. Никакой сборки, фреймворков, CMS и npm. Стили и скрипты внутри страниц, в стиле уже существующего кода.

Владелец: Artem Kholomianskyi (в текстах на русском — Артём Холомянский), JDG EVAI Consulting, Эльблонг.

Позиционирование одной строкой: **AI Implementation Consultant · Business Analyst (client side)**. Работа на стороне заказчика: аудит процессов, спецификация, надзор за реализацией. Не исполнитель автоматизаций в n8n или Make.

## Структура

```
index.html            главная
services.html         услуги
products.html         продукты (книга, ANSS, инструменты)
portfolio.html        проекты
articles.html         список статей
articles/*.html       3 статьи, тексты только на английском
audit.html            аудит отдела: Express от 3 500 zł, Department от 9 000 zł
spec.html             спецификация как услуга
metodichka.html       бесплатная методичка, самая большая страница
404.html
llms.txt  robots.txt  sitemap.xml  CNAME
img/  files/  favicon*
docs/                 ТЗ, по которым ведётся работа
```

## Как устроены языки

Три языка: EN (корень и язык по умолчанию), PL, RU.

- словарь `T` внутри страницы, ключи в атрибутах `data-i18n` (через textContent) и `data-i18n-html` (через innerHTML);
- две реализации переключателя: `applyLang()` с кнопками `.lang-btn` (index, services, products, portfolio, articles, статьи, 404) и `setLang()` с кнопками `.spec-lang-btn` (audit, spec, metodichka);
- выбор хранится в `localStorage['site-lang']`, поддерживается параметр `?lang=`;
- один URL на три языка, поэтому hreflang сейчас нет и Google видит только EN.

**Важно:** если в переводимый элемент нужно вставить ссылку, ключ переводится на `data-i18n-html`. Иначе переключение языка сотрёт ссылку вместе с разметкой.

Перевод сайта на отдельные языковые адреса `/pl/` и `/ru/` описан в `docs/TZ-i18n-migration.md`. После миграции собранные файлы не редактируются руками, правится только шаблон в `src/`.

## Абсолютные правила

1. **Не выдумывать цифры, кейсы, отзывы и клиентов.** Нет данных — ставится TBD и задаётся вопрос.
2. **Цены только с «от»:** аудит Express от 3 500 zł (7–10 дней), Department от 9 000 zł (до 10 сотрудников, 3–4 недели). Итоговая цена зависит от числа сотрудников и процессов, фиксируется после бесплатного 30-минутного разговора. Других цифр на сайте нет.
3. **Работодатель 2009–2020 не называется.** Формулировка: «оператор распределительных сетей».
4. **Почта на сайте только `artem@kholomyanskiy.com`.** Никакого gmail.
5. **Титул один и тот же** во всех текстах, включая JSON-LD и llms.txt.
6. **Дизайн-система не меняется:** существующие шрифты, цвета, сетка, компоненты. Новых не вводить.
7. **Никаких внешних зависимостей:** библиотек, шрифтов с CDN, аналитики, трекеров, форм со сторонними скриптами.
8. **Ничего не «улучшать» по пути.** Меняется только то, что описано в задаче.

## Как ведётся работа

- Задача приходит как файл ТЗ из `docs/`. Порядок действий, инварианты, проверки и стоп-условия — внутри ТЗ, они важнее общих привычек.
- Один батч или одно ТЗ = одна сессия = один коммит. После коммита обновляется `HANDOFF.md`, дальше `/clear`.
- Файлы целиком не читаются: `grep -n`, `sed -n 'a,bp'`, `head` для `<head>`. Словари `T` в контекст не выводятся.
- Если в ТЗ нет ответа — остановиться и спросить. Догадка хуже паузы.
- Перед коммитом: `python3 -m http.server 8000`, проверки из раздела «Проверка» соответствующего ТЗ, переключение всех трёх языков.
- Коммит одной строкой по-русски или по-английски, с областью: `audit: ссылки с главной и products`.

## Тексты

Сухо и конкретно. Системы, форматы, сроки, роли. Без «инновационных решений», «трансформации бизнеса» и обещаний результата.

Для публичных текстов: без тире в качестве вставок, без конструкций «не X, а Y», без слов «именно», «реально», «при этом», «данный», «комплексный».

Тексты на трёх языках пишутся сразу, иначе ключ окажется пустым.

## Где что лежит

- ТЗ — `docs/` в этом репозитории.
- Стратегия, каналы, шаблоны писем, контент-план — Google Drive, в репозиторий не кладутся.
- Книга и методичка — отдельные продукты, их тексты в репозитории не правятся.
