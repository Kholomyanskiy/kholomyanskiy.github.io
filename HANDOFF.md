# HANDOFF — миграция i18n (EVAI-SITE-2026-09-B)
Последний завершённый батч: 1 — audit.html (пилот), коммит a2a45e4 (запушен)
Мигрированы: audit
Следующий батч: 2 — index.html
Открытые проблемы: нет — `check_i18n.py` (сборка) по audit чистая на всех 3 языках; `check_i18n.py --report` по всему сайту тоже чистый
Решения и находки батча 1:
- B5/B6/B7 из отменённого блока B ТЗ-A: файла `TZ-audit-page-v1.1.md` с исходным текстом блока B нигде не нашлось (ни в Downloads, ни в репо, ни в git-истории). Артём продиктовал содержимое заново в чате: title/H1 «AI Readiness Assessment» (и переводы), строка под H1 с прежним названием без префикса, JSON-LD Service с двумя Offer (priceSpecification, minPrice 3500/9000 PLN), строка в llms.txt со ссылками на все 3 языка.
- Новый ключ словаря `h1_prev_name` (по одному на язык) — прежний текст h1_title, вынесенный под H1 отдельной строкой (`.former-name`, без цветового акцента, который есть только у `h1 em`).
- **Два реальных бага в build_i18n.py, найденные и исправленные при первой настоящей сборке (батч 0 их не поймал — только --report, без реальной генерации файлов):**
  1. `classify_and_rewrite`: ссылка на `index.html` считалась смигрированной всегда (`if page == "index"` без проверки `in migrated`), из-за чего nav_home и футер-ссылка на контакты на pl/ru ломались (`/pl/` вместо `/index.html?lang=pl`). Поправлено — проверяет migrated, как и остальные страницы.
  2. `data-i18n-html`: regex останавливался на первом `</` — для контента с вложенным `<em>...</em>` (h1_title и т.п.) в вывод попадал мусор после вложенного тега (`...AI</em> Assessment</h1>`). Заменено на posimvольный поиск закрывающего тега с учётом вложенности (`find_matching_close`, по аналогии с уже имеющимся токенайзером словаря T).
- Добавлены генерик-механизмы (нужны для всех следующих батчей, не только audit): `rewrite_lang_links` — превращает кнопки переключателя языков в реальные ссылки на соседние версии (INV-05, атрибут `data-lang` на `<a>` вместо `onclick="setLang(...)"`); `rewrite_mailto_subject` — раньше subject у mailto-кнопки писался JS-ом из `T[lang].mailto_subject`, теперь запекается в href при сборке; JSON-LD теперь поддерживает маркеры `"$t:ключ"` (резолвятся в текст словаря конкретного языка, HTML-теги вырезаются) — так Service/Offer в audit получают name/description по языку без дублирования текста вне словаря.
- `rebuild_sitemap` (шаг 4.9 ТЗ) — реализована с нуля, её не было в batch 0. Нашёл и исправил баг с неидемпотентностью при повторных запусках: `page_name_from_loc` не отрезала префикс `/pl/`/`/ru/`, из-за чего второй прогон плодил дубли записей для уже смигрированных страниц. Починено, проверено 3 прогона подряд — sitemap.xml не меняется после первого.
- Верификация: `check_i18n.py` (без ошибок) + headless Chrome по CDP (Node, `chrome.exe --headless=new`) на всех 3 адресах — 0 console/JS-ошибок, title/lang/h1/former-name/активная кнопка переключателя корректны на каждом языке, шрифты (Spectral/Inter/JetBrains Mono) загружаются, скриншоты 1280×900 визуально чистые.
- Коммит a2a45e4, запушен. **После деплоя — стоп для Артёма** (см. batch 1 в ТЗ): проверить три адреса на телефоне и компьютере, подтвердить что выглядит как раньше, только тогда переходить к батчу 2.
